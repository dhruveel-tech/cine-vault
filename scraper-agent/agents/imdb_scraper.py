from typing import Any


async def fetch_imdb_supplemental(imdb_id: str | None) -> dict[str, Any]:
    # Placeholder for Phase 2 supplemental scraping.
    # Keep this isolated so IMDb rate limits or markup changes do not break the primary TMDB flow.
    return {}
